# 03 已知问题清单

截止 2026-09-14。严重度：P0 阻塞交付 / P1 本期应修 / P2 可排期。

| ID | 严重度 | 问题 | 证据 | 建议 |
|----|--------|------|------|------|
| K-00 | **P0** | 生产形态未达到：无独立域名、无 HTTPS，超管弱口令长期可登录 | `http://123.57.134.57` 可登录 `admin/123456`；443 无响应 | 按 01、06 开通后才能宣称交付 |
| K-01 | P1 | 「批号库存」未进侧栏菜单，验收说明里的「点开批号库存」与菜单不一致 | `currentUser.menus` 无此项；URL `/business/batch-stock` 可用 | 执行 `deploy/fix-batch-stock-menu.sql` 后重新登录 |
| K-02 | P1 | 岗位账号与超管同为弱口令 `123456` | 用户 `admin/warehouse/purchase/sales/finance` 均 available=1 | 按 06 轮换并停用超管日常登录 |
| K-03 | P2 | 已过期批次仍显示质量「合格」 | `AMX20250501` 效期 2026-04-30，`expireFilter=expired` 命中 | 过期与质量状态解耦需产品确认：自动锁定或仅预警 |
| K-04 | P2 | 采购入库「确认」无原子认领，并发双确认草稿存在重复加库存风险 | 代码 `PurchaseOrderServiceImpl.confirm` 先读草稿再 `increase`；已确认单串行重入会被拒 | 对齐出库：`UPDATE ... WHERE status=草稿` 认领后再加库存 |
| K-05 | P2 | 批号库存 `increase` 非条件更新，入库加账并发安全弱于出库扣减 | `BatchStockServiceImpl.increase` vs `decreaseIfEnough` | 入库改为原子加账 |
| K-06 | P2 | 介绍页文案写「对接码上放心」，实现仅为本地 `trace_pack_relations` | 无监管 API；`previewParse` 用预置演示码 | 介绍页改为「系统内包装解析（课设/演示）」或等 04 确认后再写 |
| K-07 | P2 | 演示数据与正式数据未隔离 | 公网库即演示库；本次验收将 `CK20260907003` 签收 | 生产独立库；演示库定期重置 |
| K-08 | P2 | 员工表种子含明文口令字段 | `pharma-demo-data.sql` employees.password=`123456` | 生产禁止沿用；登录走 `sys_user` |
| K-09 | P2 | 登录页首屏对比度极低（动画未结束时几乎看不见表单） | 16:52 截图卡片接近全白 | 去掉过亮入场或提高默认不透明度 |
| K-10 | P2 | HTTP 响应缺安全头 | 无 `Strict-Transport-Security` / `Content-Security-Policy` | 随 HTTPS 一并加上 |

本期功能核验通过的项见 02，不在此重复。
